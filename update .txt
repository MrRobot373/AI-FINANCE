FinWise update summary

Repository and setup
- Cloned and worked on MrRobot373/AI-FINANCE.
- Installed and verified the frontend and backend dependencies needed to run the project locally.
- Ran the frontend at http://127.0.0.1:5173 and backend at http://127.0.0.1:8000 during testing.

Dataset upload and training fixes
- Fixed dataset upload handling so PDF and TXT files can be used as training inputs.
- Fixed training history fetch behavior that was showing "Failed to fetch files".
- Improved backend handling for uploaded file metadata and browser-submitted audio/file formats.

Login, chat, RAG, and data improvements
- Removed the old hardcoded login password value.
- Login now uses VITE_LOGIN_PASSWORD, with finwise2026 as the default fallback.
- Fixed duplicate AI loading icons in chat.
- Added clickable link rendering and Markdown cleanup in chat messages.
- Improved RAG responses for greetings, current date/time, DAX/common market-price questions, and fallback web-search permission flow.
- Added predefined income and expense categories with automatic category seeding.

Avatar planning and voice pipeline changes
- Added AVATAR_REALTIME_IMPROVEMENT_PLAN.md with the detailed realtime avatar improvement plan.
- Reworked the avatar page controls and voice mode labels.
- Replaced the earlier browser recording/websocket voice approach with a FastRTC WebRTC transport.
- Fixed the issue where the UI said "Realtime voice active..." but no audio was being processed by adding WebRTC data-channel status and microphone level feedback.
- Kept FastRTC as the browser-to-backend realtime transport.

On-device speech-to-speech pipeline
- Switched the backend voice pipeline to match the attached on-device speech-to-speech project based on asiff00/On-Device-Speech-to-Speech-Conversational-AI.
- Integrated the same core stack:
  - Pyannote VAD using pyannote/segmentation-3.0.
  - Whisper STT using openai/whisper-tiny.en.
  - Ollama LLM using qwen2.5:0.5b from the attached pipeline configuration.
  - TextChunker and AudioGenerationQueue style streaming response flow.
  - Kokoro TTS using kokoro.pth and the af_heart voice assets from the attached local pipeline folder.
- Removed the previous Moonshine/Piper-style backend voice path from the active avatar flow.
- Pinned voice dependencies to avoid NumPy 2 compatibility issues, including numpy==1.26.4, pyannote.audio==3.3.2, torch==2.4.1, torchaudio==2.4.1, and matching transformer/audio packages.

Bob avatar voice update
- Removed the previous asiff00/Pyannote/Whisper-transformers/TextChunker/Supertonic FastRTC voice runtime from the active avatar path.
- Replaced it with a direct Bob-style runtime based on OminousIndustries/Bob.
- The active browser avatar voice path now uses FastRTC WebRTC transport with Bob's core stack: RMS silence detection, Faster-Whisper tiny.en, Ollama qwen2.5:0.5b, and Kokoro KPipeline voice af_heart.
- Added Bob configuration for VAD thresholds, Whisper device/compute settings, Ollama response options, Kokoro voice, speed, and output sample rate.
- Kept FastRTC only as the browser transport so the existing web avatar can still receive microphone audio from the browser.
- Enabled GPU mode for Bob by using CUDA Faster-Whisper with float16 and CUDA Kokoro TTS.
- Replaced the CPU PyTorch install with CUDA PyTorch/torchaudio 2.11.0+cu128 for the RTX 5050 Laptop GPU.
- Added readable voice chat text inside the right-side avatar chat panel so browser voice transcripts and assistant replies appear as chat bubbles while audio plays.
- Hooked avatar gender to Kokoro voice selection: male avatar uses am_adam and female avatar uses af_heart during FastRTC voice chat.
- Fixed FastRTC avatar lip-sync so the live remote audio stream is not stopped by empty text input cleanup and drives mouth/jaw morphs from a stronger realtime audio envelope.
- Fixed female avatar lip-sync by selecting the active head/teeth morph meshes by capability and resolving morph target aliases across male and female GLB naming differences.
- Updated avatar prompts so both realtime voice and normal avatar chat stay focused on banking, personal finance, Indian rupees, and safe financial guidance.
- Improved avatar STT by upgrading Faster-Whisper from tiny.en to small.en, using beam size 3, disabling previous-text conditioning for independent voice turns, and adding finance/banking hotwords.

Verification completed
- Ran npm run build successfully.
- Ran pip check successfully after dependency pinning.
- Verified backend health readiness.
- Verified model loading for the on-device voice stack.
- Verified Kokoro TTS generation.
- Verified Pyannote and Whisper transcription.
- Ran a full WebRTC smoke test for the avatar realtime voice path.

Git status before this file
- The avatar and speech-to-speech changes were pushed to GitHub.
- Remote main was updated through commit 0e12d26 before adding this update summary file.
- Secrets and local .env values were not committed.
